import { IGraphSettingsEntity } from './model';

export const NAME = 'GRAPH';

export const graphDefaultSettings: IGraphSettingsEntity = {
    dragView: true,
    navigationButtons: true,
    zoomView: true,
};
